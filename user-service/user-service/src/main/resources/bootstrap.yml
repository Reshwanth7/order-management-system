spring:
  application:
    name: user-service  # or order-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
logging:
  level:
    org.springframework.cloud.config: DEBUG