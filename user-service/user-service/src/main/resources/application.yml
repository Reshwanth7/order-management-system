server:
  port: 9091

spring:
  application:
    name: USER-SERVICE        # uppercase — must match lb://USER-SERVICE in gateway exactly

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true